This is the root node folder, so from this directory I have done:

npm install mysql
npm install iniparser
npm install socket.io

in the resultant node_modules folder I have also placed '/jskk/' which is this repo.
some day jskk will be a module so you will just do npm install jskk :)